
numbers_sequence = input().split()
# absolute_value = []
# for num in numbers_sequence:
#     absolute_value.append(abs(float(num)))

# absolute_value = [abs(float(num))for num in numbers_sequence]
print(absolute_value)